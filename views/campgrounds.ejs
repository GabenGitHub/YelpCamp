<% include partials/header %>

<h1>This is the campground page</h1>

<% camps.forEach(el => { %>
    <p><%= el.name %></p>
    <img src="<%= el.image %>" alt="">
<% }); %>

<% include partials/footer %>